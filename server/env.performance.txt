# Phase 8 Performance Optimizations + Phase 1 Logging
# Add these to your .env file for massive speed improvements and production-ready logging!

# ==========================================
# Phase 1: Logging Configuration
# ==========================================
LOG_LEVEL=info                    # debug|info|warn|error
LOG_PRETTY=true                   # Pretty console output (true in DEV, false in PROD)
LOG_TO_FILE=true                  # Enable daily file rotation (true in DEV, false in PROD)
LOG_DIR=./logs                    # Log file directory
LOG_ROTATE_DAYS=14                # Keep logs for N days
LOG_CONSOLE=true                  # Log to console
LOG_REDACT_FIELDS=authorization,cookie,x-api-key,key,token,password,apiKey,api_key,secret

# Tracing (for future DB persistence - not used yet)
TRACE_ENABLED=true
TRACE_SAMPLE_RATE=0.0             # 0.0 in DEV (log all), 0.02 in PROD
TRACE_DEBUG_ROUTES=true           # Enable /api/debug/traces (DEV only)

# ==========================================
# Phase 8: Performance Optimizations
# ==========================================

# ⚡ DISABLE GEOCODING (if Geocoding API not enabled on your Google key)
# This stops the "Geocoding API key invalid" warnings
ENABLE_GEOCODING=false

# ⚡ ENABLE INTENT CACHING (BIGGEST WIN - 90% faster!)
CACHE_INTENT=true
CACHE_INTENT_TTL=600000

# Enable all other caching
CACHE_GEOCODING=true
CACHE_PLACES=true
CACHE_RANKING=true

# LLM optimization
LLM_TEMPERATURE=0.0
LLM_MAX_TOKENS_INTENT=200
LLM_MAX_TOKENS_ASSISTANT=150

# Metrics
METRICS_ENABLED=true

# ==========================================
# Phase 1: Candidate Pool Ranking
# ==========================================

# Candidate pool size (fetch from Google Places)
CANDIDATE_POOL_SIZE=30

# Display size (return to UI)
DISPLAY_RESULTS_SIZE=10

# Debug: include scores in response (DEV only)
DEBUG_INCLUDE_SCORE=true   # true in DEV, false in PROD


